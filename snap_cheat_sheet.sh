# Install a package with snap;
sudo snap install <package-name>

# Uninstall a package with snap;
sudo snap uninstall <package-name>

# Install updates on all snap packages;
sudo snap refresh

# Search the snap database for a package;
snap search <package-name>
